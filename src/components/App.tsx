import { Header } from '@/components/Header/Header';
import { Catalog } from '@/components/Comparison/Catalog';

export const App = () => {
	return (
		<div className="page">
			<Header />
			<Catalog />
		</div>
	);
};
